services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - ${HOME}/.apmatia:/root/.apmatia
      - ${HOME}/.config/apmatia:/root/.config/apmatia
    environment:
      - APMATIA_HOME=/root/.apmatia
