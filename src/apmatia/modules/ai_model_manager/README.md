# AI Model Manager

Local GGUF model metadata management for Apmatia.

This module keeps a separate set of model records and task-to-size preferences without changing the legacy `apmatia.lib.model_management` library.
