# Apmatia Worksim

A bundled workplace simulation module centered on a persistent org chart wiki.

The first bundled view exposes the org chart as a schema-driven collection view:

- the user is the root of the chart by default
- agents can be added beneath the root or beneath any other node
- wiki tooling can manipulate the same structure later in agent workflows

