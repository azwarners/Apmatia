# Dev Tools

This bundled module exposes developer-facing inspection tools for agents that need to understand the codebase:

- `apmatia_tree`
- `apmatia_read`
- `apmatia_trace_import`

The tools are registered into the application tool manager so agents can inspect directory trees, read source files with metadata, and trace module dependencies with cycle detection.
