# Logging

A bundled module for structured runtime logging and browsing recent application events.

The module surfaces the latest log records in the registry-backed module view layer so
users and developers can inspect page transitions, module navigation, warnings, and
exceptions without leaving the application.
